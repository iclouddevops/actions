name: CI

on:
  pull_request:
    branches: [develop]

jobs:
  ci_mlops:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: code check
        run: echo "code check"
      