name: GitHub Actions Demo

on:
  push:
    branches: [ main ]

jobs:
  run-mlops:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: iclouddevops/actions@main
      - name: check dir
        run: |
          pwd
          ls -lh


      
